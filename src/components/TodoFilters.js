import React from "react";
import { FiltersContainer, ItemsLeft } from "./TodoFilters.component";

const TodoFilters = () => {
  return (
    <FiltersContainer>
      <ItemsLeft />
    </FiltersContainer>
  );
};

export default TodoFilters;
